# Devir Notu — 09.08.2026

Bu dosya, Cowork oturumundan Claude Code'a devir. Yeni oturuma başlarken önce
`CLAUDE.md`, sonra bu dosyayı oku.

Dal: `cowork-merge` (GitHub'da güncel). Son commit: `c8ddc71`.
Güvenlik ağı: `backup/pre-merge-2026-08-09`.
`main` dalı **geride** — `cowork-merge` main'in her şeyini içeriyor artı fazlası.

---

## Bugün doğrulananlar (gerçek çalıştırma ile, iddia değil)

- **İlk gerçek mainnet transferi başarılı.** `0x9Ec1…` → `0xc4938a2e…`, 0.0005 ETH.
  Tx: `0xdc5b41e6f523f5f23a2ebddeecd0280ec8bf8e845054f66c72caade17b17b7f6`
  Zincirin tamamı çalıştı: Keychain'den key okuma → EIP-1559 imzalama → nonce →
  yayın → hash.
- **Zarf tam olarak bir kez düştü** (`spent_wei: 500000000000000`), audit log'a
  `TxAuthorized` yazıldı, kill switch atmadı.
- **Preview yan etkisiz.** Dar bir zarfta 5 ardışık `preview_transaction`: beşi de
  yetkili, `spent_wei` 0'da kaldı. (Eski kodda ikinci çağrıda kilitlenirdi.)
- **Keychain gerçek.** `~/Library/Application Support/Westron/keys/` boş,
  cüzdanlar `wallet_0x…` hesapları altında Keychain'de.
- `cargo test` 86/86, `cargo check` ve `tsc --noEmit` temiz (bizim kodumuzda).

## Bugün düzeltilen iki kritik hata

1. **`3ffe580` — private key adres sanılıyordu.** Dashboard Add Wallet modalı
   `const addr = tab === 'import' ? key : address` yapıyor ve `importWallet`'ı hiç
   çağırmıyordu. Key düz metin localStorage'a yazılıyor, sonra Alchemy'ye adres
   parametresi olarak gönderiliyordu. Artık adres backend'de key'den türetiliyor.

2. **`eec7df6` — Distribute Funds sahte "Confirmed" gösteriyordu.** İki modal
   (dashboard + wallets) `setTimeout` zinciriyle satırları Pending → Processing →
   Confirmed yapıyordu. İmzalama yok, ağ isteği yok, zarf kontrolü yok.
   Artık `src/lib/distribute.ts` üzerinden gerçek `send_eth`, sıralı gönderim,
   gerçek tx hash.

---

## AÇIK İŞ KUYRUĞU (öncelik sırasıyla)

### 1. Birleşik sürüm hiç çalıştırılmadı — ÖNCE BU
`c8ddc71` merge'ünden sonra uygulama bir kez bile açılmadı. Özellikle **Sentiment
sekmesi** ve **yeniden yazılmış dashboard** bu oturumda hiç görülmedi.
`npm run dev:tauri` → her ekranı gez, konsol hatalarına bak.

### 2. Merge borcu — taşınmayan iş
`WalletDetailClient.tsx` ve `monitor/collection/page.tsx` bütünüyle cowork-merge
sürümünden alındı (imzalama yolu ve çoklu-seçim özelliği orada; main ikisini de
kaldırmıştı). main'in `63e08be` commit'indeki şunlar **taşınmadı**:
- `NftAcceptOfferModal` (teklif kabul modalı)
- Bu iki dosyadaki NaN korumaları
Karşılaştır: `git diff c7df718 origin/main -- 'src/app/wallet/[id]/WalletDetailClient.tsx' 'src/app/monitor/collection/page.tsx'`

### 3. main dalını güncelle
`main` geride kaldı, repoya bakan eski kodu görüyor.
`git checkout main && git merge cowork-merge --ff-only && git push origin main`

### 4. Kural bazlı zamanlama (spec hazır, kod yok)
Emir'in kararı — kural formuna üç kontrol eklenecek:
- **Otomatik çalışma** — switch (kural bazlı aç/kapa)
- **Kontrol aralığı** — dropdown: 15 sn · 1 dk · 5 dk · 15 dk · 1 saat · 6 saat · günde bir. **Varsayılan 5 dk.**
- **Geçerlilik süresi** — dropdown: 1 · 6 · 12 · 24 · 48 saat · 1 hafta · 1 ay

TTL üst sınırı 168 → 720 saat çıkacak, **ama koşullu**: kural listesinde kalan süre
görünecek ve süre dolmadan uyarı gösterilecek. Bildirim olmadan sınır kaldırılmayacak.

Teknik: scheduler tik'i hızlı kalır (15 sn); her kurala `check_interval_secs` ve
`last_checked_at` eklenir, döngü yalnızca süresi dolmuş kuralları kontrol eder.
Kural başına thread açılmaz. Dokunulacak: `src-tauri/src/sniping/types.rs`
(`SnipeRule` + `SnipeRuleInput`), sniping döngüsü, TTL cap sabiti, kural formu ve
listesi, mevcut kurallar için migrasyon.

**Açık soru:** uyarı ne zaman ve nasıl? (kalan %10'da mı, son 24 saatte mi;
uygulama içi bildirim mi, liste rozeti mi)

### 5. Scheduler açık başlıyor — karar gerekiyor
Doküman "kapalı başlıyor" diyordu, gerçekte **açık** ve 30 cycle çalışmış. Aktif
kural 0 olduğu için şu an zararsız. Madde 4 uygulanırsa bu soru kendiliğinden
çözülür (global switch acil fren olur, varsayılanı kural belirler).

### 6. Dört küçük kusur
- **Sessiz gönder düğmesi.** `WalletDetailClient.runSends`, `canSend` sağlanmazsa
  hiçbir geri bildirim vermeden `return` ediyor. Hangi koşulun eksik olduğu
  söylenmeli.
- **Kendine gönderim tutarsız.** Cüzdan detayında sadece uyarı, Distribute
  modalında hedef listesi kaynağı hiç göstermiyor.
- **Zarf ön-düşümü.** `send_eth` önce limiti düşürüp sonra yayınlıyor; yayın
  başarısız olursa limit düşmüş ama işlem olmamış olur. Bu turda tetiklenmedi.
- **main'den gelen tip hataları.** `src/app/sentiment/*` örtük `any`,
  `src/lib/__tests__/formatters.test.ts` için `@types/jest` eksik.

### 7. MCP kaydı ve telefondan deneme
`tools/westron-mcp/standalone.mjs` (539 satır) commit'lendi ama Claude desktop'a
kaydedilmedi. Yönerge: `FAZ1-DEMO.md`.

### 8. Kapsam dışı — Emir kararı/hesabı gerekiyor
Apple Developer imzalama + notarization, Faz 2 Seaport gerçek alım, bağımsız
güvenlik denetimi.

---

## Ortam notları

- Uygulama: `npm run dev:tauri`. Port 3000 doluysa eski süreç kalmıştır:
  `lsof -ti:3000 -ti:7777 | xargs kill`
- Kontrol sunucusu: `127.0.0.1:7777`, Bearer token
  `~/Library/Application Support/Westron/control-token`
- Zarf kurmak (kapsam = hedef adres):
  `curl -s -H "Authorization: Bearer $T" -H 'Content-Type: application/json' -X POST http://127.0.0.1:7777/envelope -d '{"per_tx_ceiling_eth":0.001,"hard_cap_eth":0.002,"scope_addresses":["0x…"],"ttl_hours":1}'`
- Durum: `curl -s -H "Authorization: Bearer $T" http://127.0.0.1:7777/status`
- Audit: `~/Library/Application Support/Westron/audit/audit-YYYY-MM-DD.jsonl`
- `git add` sonrası lock dosyası kalırsa: `rm -f .git/index.lock; find .git/objects -name 'tmp_obj_*' -delete`
  (bu Cowork köprüsünün bıraktığı bir artıktı; Claude Code'da olmayacak)
